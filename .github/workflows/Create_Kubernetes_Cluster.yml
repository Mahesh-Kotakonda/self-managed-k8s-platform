name: Create Kubernetes Cluster

on:
  workflow_dispatch:

jobs:
  create:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify tools
        run: |
          terraform version
          ansible --version
          kubectl version --client

      - name: Placeholder - Create Cluster
        run: |
          echo "Terraform apply will go here"
