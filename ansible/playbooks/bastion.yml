---
- name: Setup bastion host (kubectl + SSH key)
  hosts: bastion
  become: true

  vars:
    kubectl_version: "v1.29.3"
    kubectl_bin_path: /usr/local/bin/kubectl

    bastion_user: ubuntu
    ssh_key_src: /home/ec2-user/k8s_key_pair.pem
    ssh_key_dest: /home/ubuntu/.ssh/k8s_key_pair.pem

  tasks:
    # -------------------------------
    # Base packages
    # -------------------------------
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Ensure required base packages are installed
      apt:
        name:
          - ca-certificates
          - curl
          - openssh-client
        state: present

    # -------------------------------
    # kubectl installation
    # -------------------------------
    - name: Download kubectl binary
      get_url:
        url: "https://dl.k8s.io/release/{{ kubectl_version }}/bin/linux/amd64/kubectl"
        dest: "{{ kubectl_bin_path }}"
        mode: "0755"
        force: yes

    - name: Verify kubectl installation
      command: kubectl version --client
      register: kubectl_version_output
      changed_when: false

    - name: Print kubectl version
      debug:
        msg: "{{ kubectl_version_output.stdout }}"

    # -------------------------------
    # SSH key setup (CRITICAL)
    # -------------------------------
    - name: Ensure .ssh directory exists for ubuntu user
      file:
        path: /home/ubuntu/.ssh
        state: directory
        owner: ubuntu
        group: ubuntu
        mode: '0700'

    - name: Copy SSH private key to bastion
      copy:
        src: "{{ ssh_key_src }}"
        dest: "{{ ssh_key_dest }}"
        owner: ubuntu
        group: ubuntu
        mode: '0600'

    - name: Ensure known_hosts file exists
      file:
        path: /home/ubuntu/.ssh/known_hosts
        state: touch
        owner: ubuntu
        group: ubuntu
        mode: '0644'
